'use strict';

var searchMatrix = function(matrix, target) {
    
};

function search(arr, tar) {
  let index = 0;
  if (arr[index] === tar) {
    return true
  } else {
    if (arr[index] > tar) {
      index++;
    } else {

    }
  }
}
